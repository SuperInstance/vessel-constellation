[package]
name = "vessel-constellation"
version = "0.1.0"
edition = "2021"
description = "N-body gravitational simulation of a fleet of vessels and their orbiting repositories"
license = "MIT"
repository = "https://github.com/SuperInstance/vessel-constellation"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
